import { styled } from '@collboard/modules-sdk';

export const FunctionBuilderConnectionStyle = styled.svg`
    position: absolute;
    pointer-events: none;
    z-index: 1;

    * {
        stroke-width: 3px;
        stroke: black;
        fill: none;
        pointer-events: none;
    }
`;
